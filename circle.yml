dependencies:
  pre:
    - sudo pip install awsebcli
deployment:
  production:
    branch: master
    commands:
      - bash ./setup-eb.sh
      - eb deploy
  staging:
    branch: staging
    commands:
      - bash ./setup-eb.sh
      - eb deploy