dependencies:
  pre:
    - sudo pip install awsebcli --ignore-installed setuptools
deployment:
  production:
    branch: master
    commands:
      - bash ./setup-eb.sh
      - eb deploy
    branch: staging
    commands:
      - bash ./setup-eb.sh
      - eb deploy